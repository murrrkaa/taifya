# Список тестов

## Список сценариев
- [x] Разбор выражения из одного числа: `42`, `-5`, `3.14`, `-2.5`
- [x] Учёт левой ассоциативности при вычитании: `1.128 - 8 + 7.5
- [x] Разбор констант: `PI`, `E`
- [x] Разбор унарных операторов: `+42`, `-(-5)`
- [x] Разбор сложения и вычитания: `1 + 2`, `10 - 3`, `1.128 + 7.5 - 8`
- [x] Разбор умножения и деления: `4 * 7.5 / 5`, `18 * 2`
- [x] Приоритет операторов: `4 + 10 * 2`, `16 - 7 / 4`
- [x] Разбор скобок: `(4 + (5 / 4)) * 2`, `2 * (3 + 4)`
- [x] Разбор встроенных функций: `abs(-5)`, `(min(2, 7, 1)`, `(max(10, 3.5)`
- [x] ``` 
         var x = readInt();
         var y = readInt();
         var result = x + y;
         print(result);
      ```
- [x] ``` 
         var radius = readInt();
         var area = PI * radius * radius;
         print(area);
      ```
- [x] ``` 
         var fahrenheit = readInt();
         var celsius = (fahrenheit - 32) * 5 / 9;
         print(celsius);
      ```
- [x] ``` 
         var fahrenheit = readInt();
         var celsius = (fahrenheit - 32) * 5 / 9;
         print(celsius);
      ```