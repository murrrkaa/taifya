# Список тестов

## Список сценариев
- [ ] Разбор выражения из одного числа: `42`, `-5`, `3.14`, `-2.5`
- [ ] Учёт левой ассоциативности при вычитании: `1.128 - 8 + 7.5
- [ ] Разбор констант: `PI`, `E`
- [ ] Разбор унарных операторов: `+42`, `-(-5)`
- [ ] Разбор сложения и вычитания: `1 + 2`, `10 - 3`, `1.128 + 7.5 - 8`
- [ ] Разбор умножения и деления: `4 * 7.5 / 5`, `18 * 2`
- [ ] Приоритет операторов: `4 + 10 * 2`, `16 - 7 / 4`
- [ ] Разбор скобок: `(4 + (5 / 4)) * 2`, `2 * (3 + 4)`
- [ ] Разбор встроенных функций: `abs(-5)`, `(min(2, 7, 1)`, `(max(10, 3.5)`
- [ ] ``` 
         var x = readInt();
         var y = readInt();
         var result = x + y;
         print(result);
      ```
- [ ] ``` 
         var radius = readInt();
         var area = PI * radius * radius;
         print(area);
      ```
- [ ] ``` 
         var fahrenheit = readInt();
         var celsius = (fahrenheit - 32) * 5 / 9;
         print(celsius);
      ```
- [ ] ``` 
         var fahrenheit = readInt();
         var celsius = (fahrenheit - 32) * 5 / 9;
         print(celsius);
      ```